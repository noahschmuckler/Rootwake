{
  "name": "rootwake",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:roots": "vite --config rootvision.config.ts",
    "build:roots": "tsc --noEmit && vite build --config rootvision.config.ts",
    "test:roots": "node scripts/test-rootvision.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test:lab": "node scripts/test-lab.mjs",
    "test:mobility": "node scripts/test-mobility.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
